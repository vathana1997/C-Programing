#include <stdio.h>

int main() {
    float m1, m2, m3, avg;

    printf("Enter marks of three subjects: ");
    scanf("%f %f %f", &m1, &m2, &m3);

    avg = (m1 + m2 + m3) / 3;

    printf("Average Marks = %.2f", avg);
    return 0;
}

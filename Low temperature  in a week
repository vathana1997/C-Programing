#include <stdio.h>

int main() {
    int i;
    float temp[7], high, low;

    for (i = 0; i < 7; i++) {
        printf("Enter temperature of day %d: ", i + 1);
        scanf("%f", &temp[i]);
    }

    high = low = temp[0];

    for (i = 1; i < 7; i++) {
        if (temp[i] > high)
            high = temp[i];
        if (temp[i] < low)
            low = temp[i];
    }

    printf("Highest = %.2f\nLowest = %.2f", high, low);

    return 0;
}
